{
  "version": 2,
  "crons": [
    {
      "path": "/api/scan-jobs",
      "schedule": "0 * * * *"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
